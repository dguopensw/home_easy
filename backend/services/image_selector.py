class ImageSelectorService:
    async def select(self, image_urls: list[str]) -> str:
        # TODO: GPT-4o Vision API로 가구가 가장 잘 보이는 이미지 1장 선정
        # 반환: 선정된 이미지 URL
        raise NotImplementedError
